## 1. Теория

- [x] 1.1 theory.md: §17 «λ(σ)-взвешивание и EDM-preconditioning» (мотивация, λ=σ² ↔ ε-предсказание, коэффициенты, взвешивание, log-normal σ, Heun) + строка оглавления

## 2. Модуль EDM

- [x] 2.1 `edm.py`: обёртка `EDMPreconditioned` (c_skip/c_out/c_in, σ-conditioning MiniUNet), предельные случаи
- [x] 2.2 `edm.py`: EDM-лосс с λ(σ) и log-normal сэмплированием σ
- [x] 2.3 `edm.py`: Heun-сэмплер по σ-сетке (Karras Alg. 2, без churn), NFE-счётчик
- [x] 2.4 Тесты: предельные случаи коэффициентов, unit-variance эффективной цели на гауссиане, NFE Heun = 2N−1

## 3. Обучение и сравнение

- [x] 3.1 `configs/default.yaml`: секция `edm` (epochs, P_mean/P_std, sigma_data, rho, сетки бюджетов)
- [x] 3.2 `scripts/train_edm.py`: обучение 20 эпох, чекпойнт `runs/edm/model_edm.pt`, лосс-кривая
- [x] 3.3 Пер-уровневый лосс EDM vs ve_long на общей оси σ; спекл-метрика обеих веток
- [x] 3.4 Бюджетный стрип EDM 40–1000 NFE (те же 8 бюджетов) + наивная ветка для сравнения

## 4. Документация и публикация

- [x] 4.1 README: раздел «EDM: починка VE» — чтение графиков, вывод по трём симптомам
- [x] 4.2 Полный прогон: тесты → train_edm → артефакты в `runs/edm/`, картинки в `images/`, коммит и пуш
